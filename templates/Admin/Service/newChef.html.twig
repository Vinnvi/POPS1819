{% extends 'Admin/Collaborateur/index.html.twig' %}

{% block body %}
    <div class="container mt-4">
        <h3>Nouveau chef de service</h3>
        {{ form_start(form) }}
            {{ form_row(form.nom) }}
            {{ form_row(form.prenom) }}
            {{ form_row(form.username) }}
            {{ form_row(form.email) }}
            {{ form_row(form.password) }}
<button class="btn btn-primary" style="margin-top: 5px">{{ button|default('Enregistrer') }}</button>
{{ form_end(form) }}
    </div>
{% endblock %}

{% block title %}

{% endblock %}